{
  "name": "book-join",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "dev": "nodemon --env-file=.env server.js",
    "kill": "taskkill /F /IM node.exe",
    "restart": "npm run kill && npm run dev",
    "db:create": "createdb blog-api",
    "db:setup": "psql -U postgres -d blog-api -f database/schema.sql",
    "db:seed": "psql -U postgres -d blog-api -f database/seed.sql"
  },
  "keywords": [],
  "type": "module",
  "author": "",
  "license": "ISC",
  "dependencies": {
    "bcrypt": "^6.0.0",
    "dotenv": "^17.2.3",
    "express": "^5.2.1",
    "express-rate-limit": "^8.2.1",
    "express-validator": "^7.3.1",
    "jsonwebtoken": "^9.0.3",
    "nodemon": "^3.1.11",
    "pg": "^8.17.0"
  }
}
